apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: phpmyadmin-ingress
spec:
  rules:
  - host: phpmyadmin.laravel.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: auth-proxy-service
            port:
              number: 80